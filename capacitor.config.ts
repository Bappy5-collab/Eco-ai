import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: "com.ecoai.app",
  appName: "Eco AI",
  webDir: "public" // or "out" if static export
};

export default config;
